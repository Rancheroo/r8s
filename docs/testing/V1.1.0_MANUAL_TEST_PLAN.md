# R8S v1.1.0 Manual Test Plan

**Version:** v1.1.0  
**Date:** 2026-03-03  
**Branch:** `feature/v1.1-bundle-changes`  
**Purpose:** Comprehensive manual testing checklist for production release

---

## Prerequisites

```bash
# Build the binary
cd /home/bradmin/.openclaw/workspace/r8s
make build

# Verify version
./bin/r8s version
# Expected: v1.0.1-XX-gXXXXXXX (dev version on branch)

# Test bundles location
export TEST_BUNDLE_NEW="/home/bradmin/Downloads/logBundles/r8s-cp-wlp7h-lhvgq-2026-03-03_03_49_04"
export TEST_BUNDLE_OLD="/home/bradmin/Downloads/logBundles/r8s-cp-wlp7h-lhvgq-2026-02-03_05_38_57"
```

---

## Test Suite 1: Core Commands

### 1.1 Version Command
```bash
./bin/r8s version
```
**Expected:** Version string displayed (e.g., `r8s v1.0.1-XX-gXXXXXXX`)
**Status:** ⬜ PASS / ⬜ FAIL

### 1.2 Help Command
```bash
./bin/r8s --help
```
**Expected:** Help text with available commands and examples
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Suite 2: Bundle Analysis (NEW format bundle)

### 2.1 Analyze Command
```bash
./bin/r8s analyze "$TEST_BUNDLE_NEW"
```
**Expected:**
- [ ] Shows bundle type (RKE2)
- [ ] Shows health percentage (93%)
- [ ] Lists critical issues (7 expected)
- [ ] Lists warning issues (225 expected)
- [ ] Shows cowboy-themed loading messages
**Status:** ⬜ PASS / ⬜ FAIL

### 2.2 Analyze with JSON Output
```bash
./bin/r8s analyze "$TEST_BUNDLE_NEW" --format=json | jq '.bundle_type'
```
**Expected:** Valid JSON with `bundle_type: "rke2"`
**Status:** ⬜ PASS / ⬜ FAIL

### 2.3 Ask Command (Natural Language)
```bash
./bin/r8s ask "$TEST_BUNDLE_NEW" "what issues are present?"
```
**Expected:**
- [ ] Returns non-empty response
- [ ] Lists actual issues from bundle (OOMKilled, CrashLoopBackOff, etc.)
- [ ] Provides context for each issue
**Status:** ⬜ PASS / ⬜ FAIL

### 2.4 Ask with Specific Question
```bash
./bin/r8s ask "$TEST_BUNDLE_NEW" "show me crash loops"
```
**Expected:** Lists pods in CrashLoopBackOff state
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Suite 3: Resource Queries (kubectl-style)

### 3.1 Get Pods
```bash
./bin/r8s get pods "$TEST_BUNDLE_NEW"
```
**Expected:**
- [ ] Table format with columns: NAMESPACE, NAME, READY, STATUS, RESTARTS, AGE
- [ ] Lists all pods from bundle
- [ ] Includes pods from cattle-system namespace
**Status:** ⬜ PASS / ⬜ FAIL

### 3.2 Get Pods with Namespace Filter
```bash
./bin/r8s get pods "$TEST_BUNDLE_NEW" -n cattle-system
```
**Expected:** Only pods from cattle-system namespace
**Status:** ⬜ PASS / ⬜ FAIL

### 3.3 Get Nodes
```bash
./bin/r8s get nodes "$TEST_BUNDLE_NEW"
```
**Expected:**
- [ ] Lists all nodes
- [ ] Shows status, roles, age, version
**Status:** ⬜ PASS / ⬜ FAIL

### 3.4 Logs Command
```bash
./bin/r8s logs "$TEST_BUNDLE_NEW" calico-node-88rjf
```
**Expected:**
- [ ] Shows pod logs
- [ ] Includes timestamps
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Suite 4: Export Formats

### 4.1 Export SARIF
```bash
./bin/r8s export "$TEST_BUNDLE_NEW" --format=sarif --output=/tmp/test.sarif.json
head -20 /tmp/test.sarif.json
```
**Expected:**
- [ ] Valid SARIF JSON structure
- [ ] Contains `runs`, `tool`, `results`
- [ ] Includes rule definitions
**Status:** ⬜ PASS / ⬜ FAIL

### 4.2 Export Markdown
```bash
./bin/r8s export "$TEST_BUNDLE_NEW" --format=markdown --output=/tmp/test.md
head -30 /tmp/test.md
```
**Expected:**
- [ ] Human-readable markdown
- [ ] Headers and lists formatted correctly
**Status:** ⬜ PASS / ⬜ FAIL

### 4.3 Export JSON
```bash
./bin/r8s export "$TEST_BUNDLE_NEW" --format=json --output=/tmp/test.json
jq '.findings | length' /tmp/test.json
```
**Expected:** Valid JSON with findings array
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Suite 5: Bundle Validation

### 5.1 Validate New Bundle
```bash
./bin/r8s validate "$TEST_BUNDLE_NEW"
```
**Expected:**
- [ ] Shows bundle health (93%)
- [ ] Lists missing files (systemlogs/dmesg expected)
- [ ] Shows category breakdown
**Status:** ⬜ PASS / ⬜ FAIL

### 5.2 Validate Old Bundle
```bash
./bin/r8s validate "$TEST_BUNDLE_OLD"
```
**Expected:**
- [ ] Shows bundle health
- [ ] Different missing files than new bundle
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Suite 6: Test Cluster Command

### 6.1 Test Cluster (Table Format)
```bash
./bin/r8s test-cluster "$TEST_BUNDLE_NEW"
```
**Expected:**
- [ ] Runs 7 diagnostic tests
- [ ] Shows PASS/FAIL for each test
- [ ] Pod Health Check shows FAIL (27 unhealthy pods expected)
- [ ] Exit code: 1 (because issues found)
**Status:** ⬜ PASS / ⬜ FAIL

### 6.2 Test Cluster (JSON Format)
```bash
./bin/r8s test-cluster "$TEST_BUNDLE_NEW" --format=json | jq '.[0].status'
```
**Expected:** Valid JSON array with test results
**Status:** ⬜ PASS / ⬜ FAIL

### 6.3 Test Cluster (Summary Format)
```bash
./bin/r8s test-cluster "$TEST_BUNDLE_NEW" --format=summary
```
**Expected:** Brief summary (e.g., "FAIL: X test(s) failed")
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Suite 7: Bundle Format v1.1 Features

### 7.1 Virt Detection (New Bundle)
```bash
./bin/r8s analyze "$TEST_BUNDLE_NEW" 2>&1 | grep -i "virt\|kvm"
# Or check the code path directly via tests
go test ./internal/bundle/... -v -run "DetectVirt_KVM"
```
**Expected:** 
- [ ] Integration test shows VirtType: kvm
- [ ] ParseSystemHealth returns VirtType="kvm"
**Status:** ⬜ PASS / ⬜ FAIL

### 7.2 Versions File Parsing
```bash
# Run integration test
go test ./internal/bundle/... -v -run "TestParseVersions_Success"
```
**Expected:**
- [ ] Parses collection date
- [ ] Parses hostname, kernel, OS
- [ ] Parses RKE2 version
- [ ] Parses container images (18 RKE2, 3 Cattle)
- [ ] Parses helm releases (11 expected)
**Status:** ⬜ PASS / ⬜ FAIL

### 7.3 Memory File Parsing (Gi Units)
```bash
go test ./internal/bundle/... -v -run "TestParseSystemHealth_NewMemoryFormat"
```
**Expected:**
- [ ] Parses "3.8Gi" format correctly
- [ ] Calculates memory percentage (52.6%)
**Status:** ⬜ PASS / ⬜ FAIL

### 7.4 Path Fallback (Old Bundle)
```bash
go test ./internal/bundle/... -v -run "TestIntegration_OldBundleFormat"
```
**Expected:**
- [ ] Memory parsing works via freem file
- [ ] DMesg parsing works from systemlogs location
- [ ] Versions file correctly not found
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Suite 8: Backward Compatibility

### 8.1 Full Analysis on Old Bundle
```bash
./bin/r8s analyze "$TEST_BUNDLE_OLD"
```
**Expected:**
- [ ] Works without errors
- [ ] Finds issues (may be different than new bundle)
- [ ] Health percentage displayed
**Status:** ⬜ PASS / ⬜ FAIL

### 8.2 Test Cluster on Old Bundle
```bash
./bin/r8s test-cluster "$TEST_BUNDLE_OLD"
```
**Expected:** All tests run, results shown
**Status:** ⬜ PASS / ⬜ FAIL

### 8.3 Get Pods on Old Bundle
```bash
./bin/r8s get pods "$TEST_BUNDLE_OLD" | head -10
```
**Expected:** Pod list displayed
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Suite 9: Error Handling & Edge Cases

### 9.1 Non-existent Bundle Path
```bash
./bin/r8s analyze /nonexistent/path 2>&1
```
**Expected:** Friendly error message (no panic)
**Status:** ⬜ PASS / ⬜ FAIL

### 9.2 Empty Directory
```bash
mkdir -p /tmp/empty-bundle
./bin/r8s analyze /tmp/empty-bundle 2>&1
```
**Expected:** Graceful handling, error message
**Status:** ⬜ PASS / ⬜ FAIL

### 9.3 Unknown Command
```bash
./bin/r8s unknowncommand 2>&1
```
**Expected:** "Unknown command" message with suggestions
**Status:** ⬜ PASS / ⬜ FAIL

### 9.4 Typo Suggestion
```bash
./bin/r8s anlyze "$TEST_BUNDLE_NEW" 2>&1
```
**Expected:** Suggests "analyze" with "Did you mean?"
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Suite 10: Performance

### 10.1 Large Bundle Analysis
```bash
time ./bin/r8s analyze "$TEST_BUNDLE_NEW"
```
**Expected:**
- [ ] Completes in < 5 seconds
- [ ] No timeout errors
**Status:** ⬜ PASS / ⬜ FAIL

---

## Test Summary

| Suite | Tests | Passed | Failed | Status |
|-------|-------|--------|--------|--------|
| 1. Core Commands | 2 | | | |
| 2. Bundle Analysis | 4 | | | |
| 3. Resource Queries | 4 | | | |
| 4. Export Formats | 3 | | | |
| 5. Bundle Validation | 2 | | | |
| 6. Test Cluster | 3 | | | |
| 7. v1.1 Features | 4 | | | |
| 8. Backward Compatibility | 3 | | | |
| 9. Error Handling | 4 | | | |
| 10. Performance | 1 | | | |
| **TOTAL** | **30** | | | |

**Overall Status:** ⬜ READY FOR RELEASE / ⬜ NEEDS FIXES

---

## Sign-off

**Tester:** _______________  
**Date:** _______________  
**Branch:** `feature/v1.1-bundle-changes`  
**Commit:** _______________

**Notes:**
```
[Space for tester notes, issues found, etc.]
```

---

## Quick Reference

### Build
```bash
make build
```

### Run All Tests
```bash
go test ./... -cover
```

### Run Integration Tests Only
```bash
go test ./internal/bundle/... -v -run "Integration"
```

### Check Test Coverage
```bash
go test ./internal/bundle/... -coverprofile=coverage.out
go tool cover -func=coverage.out | grep total
```

### Bundle Locations
- New format: `/home/bradmin/Downloads/logBundles/r8s-cp-wlp7h-lhvgq-2026-03-03_03_49_04`
- Old format: `/home/bradmin/Downloads/logBundles/r8s-cp-wlp7h-lhvgq-2026-02-03_05_38_57`
