# v1.1 Bundle Format Changes - Implementation Sprint

**Branch:** `feature/v1.1-bundle-changes`  
**Status:** IN PROGRESS  
**Started:** 2026-03-03

---

## Sprint Goal

Implement support for the new bundle format changes identified in `BUNDLE_FORMAT_CHANGES_v1.1.md`:
1. Add parsers for new files (systemd-detect-virt, versions)
2. Update path mappings for moved/changed files
3. Handle new directory structures (poddescribe/, sysstat-data/)
4. Maintain backward compatibility with old bundle format

---

## Completed ✅

### Phase 1: Documentation
- [x] Create `BUNDLE_FORMAT_CHANGES_v1.1.md` - Complete audit of format changes
- [x] Create sprint tracking document (this file)
- [x] Commit documentation to branch

### Phase 2: Virt Detection
- [x] Add `VirtType` field to `SystemHealthInfo` struct
- [x] Parse `systeminfo/systemd-detect-virt` file
- [x] Add tests for virt type parsing
- [x] Commit: `f3a8dcd` - feat: Add virtualization detection

---

## In Progress 🔄

### Phase 3: Versions File Parser
**Task:** Create parser for root-level `versions` file

The `versions` file contains:
```
- System info (date, memory, kernel, OS)
- RKE2 version
- Container images (rke2, cattle, system-upgrade-controller)  
- Helm releases (rke2-* charts)
- Fleet agent info
```

**Files to create/modify:**
- `internal/bundle/versions.go` - New parser
- `internal/bundle/versions_test.go` - Tests
- `internal/bundle/types.go` - Add VersionInfo struct

---

## Backlog 📋

### Phase 4: Path Updates
- [ ] Update `paths.go` - Add root-level version file path
- [ ] Update `paths.go` - Handle `freem` → `memory` migration
- [ ] Update `paths.go` - Handle `dmesg` location change
- [ ] Update `systeminfo.go` - Support both `freem` and `memory` files
- [ ] Update `dmesg.go` - Support both old and new dmesg locations

### Phase 5: New Directory Support
- [ ] Add `PodDescribeParser` for per-namespace pod describe files
- [ ] Update path resolver for `poddescribe/` directory
- [ ] Add `SysstatParser` for sar/sa performance data (optional)

### Phase 6: Cleanup
- [ ] Remove cloud-init log parsers (if they exist)
- [ ] Update file existence checks for backward compatibility
- [ ] Add integration tests with new bundle format
- [ ] Update CHANGELOG.md

---

## Implementation Notes

### Backward Compatibility Strategy
All parsers should implement fallback logic:
```go
// Try new path first
if data, err := os.ReadFile(newPath); err == nil {
    return parseNewFormat(data)
}
// Fall back to old path
if data, err := os.ReadFile(oldPath); err == nil {
    return parseOldFormat(data)
}
```

### Testing Strategy
1. Unit tests for each new parser
2. Integration tests with both old and new bundle formats
3. Test with actual sample bundles from `/home/bradmin/Downloads/logBundles/`

---

## Bundle Samples Available

```
/home/bradmin/Downloads/logBundles/
├── r8s-cp-wlp7h-lhvgq-2026-03-03_03_49_04/  <- NEW format (use for testing)
├── r8s-cp-wlp7h-lhvgq-2026-02-03_05_38_57/  <- OLD format (backward compat)
└── [many more old bundles]
```

---

## Daily Log

### 2026-03-03 (Tuesday)
- **Morning:** Analyzed bundle differences, documented format changes
- **Afternoon:** Started v1.1 branch, committed documentation and virt detection
- **Next:** Build versions file parser

