# v0.6.0 "Diagnostic-First Intelligence" Plan

**Status**: Planning  
**Prerequisites**: v0.5.10, v0.5.11, v0.5.12 (foundation releases)  
**Target**: After v0.5.x foundation complete  
**Focus**: Full diagnostic-first overhaul with inline context

---

## v0.5.x Prerequisites (Foundation Releases)

The following releases lay the groundwork for v0.6.0's diagnostic-first overhaul. Each adds backend capabilities without changing the UI.

### v0.5.10 "Data Source Enrichment"

**Goal**: Parse additional bundle data without UI changes

**New Parsers:**
1. **Enhanced ETCD Parser** (`internal/bundle/etcd.go`)
   - Parse `memberlist` → MemberCount, LeaderID
   - Parse `status` → DBSize, DBSizeInUse
   - Calculate compaction recommendations
   - Identify slow queries from logs

2. **Node Conditions Parser** (`internal/bundle/nodes.go` - NEW)
   - Parse kubectl node YAML/JSON
   - Extract ReadyCondition, MemoryPressure, DiskPressure, PIDPressure
   - Parse node resource capacity/allocatable
   - Track node taints and cordons

3. **DataSource Interface Expansion**
   - Add `GetNodeConditions(nodeName string) (*NodeConditions, error)`
   - Add `GetEtcdDetails() (*EtcdDetails, error)`
   - Implement in BundleDataSource

**Deliverables:**
- [ ] Extended `EtcdHealthInfo` struct with new fields
- [ ] New `NodeConditions` struct and parser
- [ ] Updated DataSource interface
- [ ] Unit tests for all new parsers
- [ ] No UI changes (backend only)

---

### v0.5.11 "Kubelet & OOM Analysis"

**Goal**: Add advanced detection capabilities for diagnostic context

**New Parsers:**
1. **Kubelet Log Parser** (`internal/bundle/kubelet.go` - NEW)
   - Parse `journald/kubelet.log` for common errors
   - Detect: "not healthy", "failed to sync pod", "CNI failed"
   - Track affected pod counts per error type
   - Extract last occurrence timestamp

2. **OOM Root Cause Analyzer** (`internal/bundle/oom.go` - NEW)
   - Parse pod spec for memory requests/limits
   - Parse events for OOM kill messages
   - Distinguish container OOM vs node OOM
   - Calculate memory utilization at kill time

3. **Container Resource Parser** (`internal/bundle/resources.go` - NEW)
   - Extract CPU/memory requests per container
   - Extract CPU/memory limits per container
   - Calculate request/limit ratios
   - Identify guaranteed/burstable/best-effort QoS

**Deliverables:**
- [ ] `KubeletIssue` struct with error patterns
- [ ] `OOMAnalysis` struct with root cause
- [ ] `ResourceSpec` struct for limits/requests
- [ ] Detection helper functions
- [ ] Unit tests for all analyzers
- [ ] No UI changes (backend only)

---

### v0.5.12 "Diagnostic Context Types"

**Goal**: Create data structures for inline diagnostic display

**New Types:**
1. **DiagnosticContext** (`internal/tui/diagnostics.go` - NEW)
   ```go
   type DiagnosticContext struct {
       RootCause      string   // "Container exceeded memory limit"
       Recommendation string   // "Increase limit to 2Gi or optimize app"
       RelatedData    []string // ["Memory: 980Mi/1Gi", "Node: 95% memory"]
   }
   ```

2. **AttentionItem Extension**
   - Add `DiagnosticContext *DiagnosticContext` field
   - Populated during detection phase
   - Ready for inline display in v0.6.0

3. **Context Generators** (`internal/tui/diagnostics.go`)
   - `generateCrashLoopContext(pod, events) *DiagnosticContext`
   - `generateOOMContext(pod, analysis) *DiagnosticContext`
   - `generateImagePullContext(pod, events) *DiagnosticContext`
   - `generateNodeContext(node, conditions) *DiagnosticContext`
   - `generateEtcdContext(etcdDetails) *DiagnosticContext`

4. **Recommendation Engine** (`internal/tui/recommendations.go` - NEW)
   - Map issue patterns to actionable suggestions
   - Prioritize recommendations (immediate/investigate/optimize)
   - Format recommendations with emoji indicators

**Deliverables:**
- [ ] DiagnosticContext struct
- [ ] Extended AttentionItem
- [ ] 5+ context generator functions
- [ ] Recommendation mapping system
- [ ] Unit tests for context generation
- [ ] No UI changes (data only)

---

## Vision

Apply "5-Second Rule" to diagnostic panel: Operator should decide on action within **5 seconds** of viewing the panel.

### Core Principles (from LESSONS-LEARNED.md)

1. **"Show, Don't Ask"** - Display relevant information automatically
2. **"Truth Only™"** - Never show fake or misleading data
3. **"Maximum Information Extraction"** - Use all available bundle data
4. **"r8s interprets, user acts"** - Provide intelligence, not just information

---

## Phase 1: Diagnostic Panel Tightening

### Goals
- Remove noise/fluff from diagnostic panel
- Focus on ONLY actionable information
- Apply "5-Second Rule" - quick decision-making

### Changes

#### REMOVE: External Tools Section
**Current**: 🛠️ EXTERNAL TOOLS shows lnav and kubectl hints  
**Problem**: Not actionable in bundle mode, adds clutter  
**Solution**: Move to help modal ('?' key)  
**Impact**: One less section, tighter panel

#### SHOW ONLY: Failing Containers
**Current**: Shows ALL containers (healthy + failing)  
**Problem**: Healthy containers add noise  
**Solution**: Show only containers with issues  
**Format**:
```
━━━ FAILING CONTAINERS ━━━
❌ main-container: BackOff (x47) - CrashLoopBackOff
❌ sidecar: ImagePullBackOff - registry auth failed
```

#### SHOW ONLY: Warning Events
**Current**: Shows Normal + Warning events mixed  
**Problem**: Normal events ("Started", "Pulled") add noise  
**Solution**: Show only Warning events (unless none exist)  
**Format**:
```
━━━ WARNING EVENTS ━━━
⚠️ BackOff: Back-off restarting failed container (x47)
⚠️ Failed: Image pull failed - registry.example.com [truncated]
```

#### TRUNCATE: Event Messages
**Current**: Full event messages can span multiple lines  
**Problem**: Hard to scan, breaks "5-second rule"  
**Solution**: Truncate at 80 chars + "(truncated)" indicator  
**Impact**: More scannable, consistent line heights

#### NUMBER: Action Suggestions
**Current**: Bullet list of actions  
**Enhancement**: Number them for quick reference  
**Format**:
```
━━━ ACTIONS ━━━
1. Check container logs for exit reason
2. Verify registry credentials (imagePullSecrets)
3. Review resource limits if OOM suspected
```

### New Panel Layout (5-Second Format)

```
━━━ DIAGNOSIS ━━━
🔴 CRASH LOOP - Container failing to start (47 restarts)
   → Check container exit codes in events below

━━━ FAILING CONTAINERS ━━━
❌ main-container: BackOff (x47) - CrashLoopBackOff  
❌ sidecar: ImagePullBackOff - registry auth failed

━━━ WARNING EVENTS ━━━
⚠️ BackOff: Back-off restarting failed container (x47)
⚠️ Failed: Image pull failed - registry.example.com [truncated]

━━━ ACTIONS ━━━
1. Check container logs for exit reason
2. Verify registry credentials (imagePullSecrets)  
3. Review resource limits if OOM suspected

[l]=logs  [Ctrl+P]=previous  [Esc]=back
```

### Implementation Checklist

- [ ] Move External Tools section to help modal
- [ ] Filter containers to show only failing ones
- [ ] Filter events to show only Warning type
- [ ] Add event message truncation at 80 chars
- [ ] Number action suggestions
- [ ] Update help modal with External Tools info
- [ ] Test with various pod states (CrashLoop, OOM, ImagePull, etc.)

---

## Phase 2: Cluster Events Drill-Down (v0.5.9 Known Limitation)

### Problem Discovered in v0.5.9 Testing

**Issue**: Dashboard shows aggregated cluster events (e.g., "190× BackOff", "118× Failed") but provides no way to see which pods are affected.

**Root Cause**: v0.5.9 removed the expansion feature (►/▼ indicators) that showed affected pod lists. This was intentional to simplify navigation, but created a gap for cluster event navigation.

**Current Behavior**:
```
WARNING:
6. 🔴 190× BackOff    Warning events    cluster     <-- Enter does nothing useful
7. 🔴 118× Failed     Warning events    cluster     <-- No way to see pod list
```

**Expected Behavior**: Pressing Enter on cluster event items should show which pods are affected.

### Solution: Cluster Events Diagnostic Panel

Create a new diagnostic panel type for cluster-wide events that shows affected pods.

**New Panel Layout**:
```
━━━ CLUSTER EVENT: BackOff ━━━
⚠️ 190 occurrences across 12 pods

━━━ AFFECTED PODS ━━━
1. 💀 nginx-pod-abc       47 events    kube-system    CrashLoopBackOff
2. 💀 api-server-xyz      23 events    default        CrashLoopBackOff  
3. 💀 coredns-def         19 events    kube-system    CrashLoopBackOff
4. 🔥 worker-ghi          15 events    prod           BackOff
5. 🔥 db-jkl              12 events    prod           BackOff
...

━━━ ACTIONS ━━━
1. Press number (1-9) to jump to pod diagnostic panel
2. Review event message patterns for common root cause
3. Check if pods share same namespace or image

[1-9]=select pod  [Esc]=back to dashboard
```

### Implementation Details

1. **Detect cluster event items**: Check if `AttentionItem.ResourceType == "event"` or `"cluster"`
2. **Create new view handler**: `handleClusterEventDrillDown()`
3. **Parse affected pods** from `AttentionItem.AffectedPods` and `AffectedPodCounts`
4. **Number pods 1-9** for quick keyboard navigation
5. **Allow Enter** on numbered pod to jump to that pod's diagnostic panel

### Files to Modify
- `internal/tui/handlers.go` - Add cluster event Enter handler
- `internal/tui/attention.go` - Add `renderClusterEventPanel()`
- `internal/tui/app.go` - Add ViewClusterEvent view type (optional)

### Success Criteria
- ✅ Pressing Enter on "190× BackOff" shows list of affected pods
- ✅ Can navigate from cluster event → pod list → individual pod diagnostic
- ✅ Maintains simplified navigation (no expansion mode)
- ✅ Keyboard-driven workflow (numbers 1-9 for pod selection)

---

## Phase 3: Container Detection (Deferred from v0.5.9)

### Problem
Currently, container list is not populated from pod spec (only shows current container).

### Solution
Parse `kubectl describe pod` output or pod spec to extract container names.

### Implementation
1. Add `GetPodSpec()` method to DataSource interface
2. Parse JSON/YAML spec to extract container list
3. Populate `containers` field in logsMsg with all containers
4. Enable proper container cycling with 'c' key

### Files to Modify
- `internal/datasource/interface.go` - Add GetPodSpec()
- `internal/datasource/bundle.go` - Implement pod spec parsing
- `internal/tui/fetch.go` - Call GetPodSpec() instead of hardcoding

---

## Success Criteria

### Quantitative
- ✅ Diagnostic panel has **4 sections** (down from 6)
- ✅ Event messages **≤80 chars** per line
- ✅ Show **0 healthy containers** when debugging failures
- ✅ Show **0 Normal events** when Warning events exist

### Qualitative  
- ✅ **5-Second Rule**: Operator makes decision in 5 seconds
- ✅ **Zero Fluff**: Every line is actionable
- ✅ **Scannable**: Consistent line heights, no wrapping
- ✅ **Focused**: Shows only relevant data for the problem

---

## Testing Plan

### Test Cases
1. **CrashLoopBackOff pod** - Should show failing container only
2. **ImagePullBackOff pod** - Should show registry error, no healthy containers
3. **OOMKilled pod** - Should show memory-related events only
4. **Multi-container pod** - Should show only failing container, not healthy sidecar
5. **Pod with long event messages** - Should truncate at 80 chars

### Validation
- Screenshot diagnostic panels before/after
- Count sections (6 → 4)
- Measure time to action decision (<5 seconds)
- User feedback: "Is this panel actionable?"

---

## Future Enhancements (Post-v0.6.0)

### Event Grouping
- Group repeated events: "Image pull failed (x6)" instead of 6 separate lines
- Reduces clutter, increases signal

### Contextual Action Shortcuts
- Press number key to execute suggested action
- Example: Press '1' to open full logs
- Requires action automation framework

### Smart Event Highlighting
- Highlight most critical event in red
- Secondary events in yellow
- Visual hierarchy for quick scanning

---

## Appendix: Design Decisions

### Why Remove External Tools Section?

**Reasoning**: In bundle mode, lnav and kubectl commands are references, not actions. They don't help the immediate diagnosis. Moving to help modal keeps them accessible without cluttering the main panel.

**User Flow**: Operator sees diagnostic panel → realizes they need kubectl → presses '?' → sees kubectl command in help.

### Why Show Only Failing Containers?

**Reasoning**: Healthy containers are noise when debugging a pod failure. The operator needs to focus on what's broken, not what's working.

**Exception**: If ALL containers are healthy but pod is in Error state, show all containers with "⚠️ No failing containers detected - check pod events."

### Why Show Only Warning Events?

**Reasoning**: Normal events ("Scheduled", "Started", "Pulled") describe successful operations. When debugging, we need to see what FAILED, not what succeeded.

**Fallback**: If no Warning events exist, show recent Normal events with note: "No warning events - recent activity shown."

---

## References

- **LESSONS-LEARNED.md** - Core principles and past learnings
- **FUTURE_WORK.md** - Deferred enhancements
- **v0.5.7_HANDOFF.md** - Current state documentation
- **CHANGELOG.md** - v0.5.9 baseline for this work
