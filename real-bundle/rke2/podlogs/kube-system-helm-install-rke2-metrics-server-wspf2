if [[ ${KUBERNETES_SERVICE_HOST} =~ .*:.* ]]; then
	echo "KUBERNETES_SERVICE_HOST is using IPv6"
	CHART="${CHART//%\{KUBERNETES_API\}%/[${KUBERNETES_SERVICE_HOST}]:${KUBERNETES_SERVICE_PORT}}"
else
	CHART="${CHART//%\{KUBERNETES_API\}%/${KUBERNETES_SERVICE_HOST}:${KUBERNETES_SERVICE_PORT}}"
fi

set +v -x
+ [[ '' == v2 ]]
+ shopt -s nullglob
+ [[ -f /config/ca-file.pem ]]
+ [[ -f /tmp/ca-file.pem ]]
+ [[ false == true ]]
+ [[ false == true ]]
+ [[ -n '' ]]
+ helm_content_decode
+ set -e
+ ENC_CHART_PATH=/chart/rke2-metrics-server.tgz.base64
+ CHART_PATH=/tmp/rke2-metrics-server.tgz
+ [[ ! -f /chart/rke2-metrics-server.tgz.base64 ]]
+ base64 -d /chart/rke2-metrics-server.tgz.base64
+ CHART=/tmp/rke2-metrics-server.tgz
+ set +e
+ [[ install != delete ]]
+ helm_repo_init
+ grep -q -e 'https\?://'
+ [[ /tmp/rke2-metrics-server.tgz == stable/* ]]
+ [[ -n '' ]]
+ helm_update install --set-string global.clusterCIDR=10.42.0.0/16 --set-string global.clusterCIDRv4=10.42.0.0/16 --set-string global.clusterDNS=10.43.0.10 --set-string global.clusterDomain=cluster.local --set-string global.rke2DataDir=/var/lib/rancher/rke2 --set-string global.serviceCIDR=10.43.0.0/16 --set-string global.systemDefaultIngressClass=ingress-nginx
++ tr '[:upper:]' '[:lower:]'
++ helm ls --all -f '^rke2-metrics-server$' --namespace kube-system --output json
++ jq -r '"\(.[0].chart),\(.[0].status)"'
+ LINE=null,null
+ IFS=,
+ read -r INSTALLED_VERSION STATUS _
+ VALUES=
+ [[ install = delete ]]
+ [[ null =~ ^(|null)$ ]]
+ [[ null =~ ^(|null)$ ]]
+ echo 'Installing helm chart'
+ helm install --set-string global.clusterCIDR=10.42.0.0/16 --set-string global.clusterCIDRv4=10.42.0.0/16 --set-string global.clusterDNS=10.43.0.10 --set-string global.clusterDomain=cluster.local --set-string global.rke2DataDir=/var/lib/rancher/rke2 --set-string global.serviceCIDR=10.43.0.0/16 --set-string global.systemDefaultIngressClass=ingress-nginx rke2-metrics-server /tmp/rke2-metrics-server.tgz
NAME: rke2-metrics-server
LAST DEPLOYED: Tue Feb  3 05:14:25 2026
NAMESPACE: kube-system
STATUS: deployed
REVISION: 1
TEST SUITE: None
NOTES:
***********************************************************************
* Metrics Server                                                      *
***********************************************************************
  Chart version: 3.13.004
  App version:   0.8.0
  Image tag:     rancher/hardened-k8s-metrics-server:v0.8.0-build20251204
***********************************************************************
+ exit
